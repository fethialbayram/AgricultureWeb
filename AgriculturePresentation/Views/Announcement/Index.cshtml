@using EntityLayer.Concrete
@model List<Announcement>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Duyuru Listesi</h4>
            <div class="table-responsive pt-3">
                <table class="table table-bordered">
                    <thead>
                    <th> #</th>
                    <th>Başlık</th>
                    <th>Tarih</th>
                    <th>Sil</th>
                    <th>Düzenle</th>
                    <th>Aktif Yap</th>
                    <th>Pasif Yap</th>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.Id</td>
                                <td>@item.Title</td>
                                <td>@item.Date</td>
                                <td><a href="/Announcement/DeleteAnnouncement/@item.Id" class="btn btn-outline-danger">Sil</a></td>
                                <td><a href="/Announcement/EditAnnouncement/@item.Id" class="btn btn-outline-success">Güncelle</a></td>
                                <td><a href="/Announcement/EditAnnouncement/@item.Id" class="btn btn-outline-warning">Aktif Yap</a></td>
                                <td><a href="/Announcement/EditAnnouncement/@item.Id" class="btn btn-outline-dark   ">Pasif Yap</a></td>
                            </tr>
                        }
                    </tbody>
                </table>
                <br />
                <a href="/Announcement/AddAnnouncement" class="btn btn-outline-info">Yeni Duyuru Girişi</a>
            </div>
        </div>
    </div>
</div>


